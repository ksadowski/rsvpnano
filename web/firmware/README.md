# Web Firmware Artifacts

The browser flasher expects merged ESP32-S3 binaries in this folder:

- `rsvp-nano.bin`

These files are generated by:

```sh
python3 tools/export_web_firmware.py
```

The generated `.bin` files are intentionally ignored by Git. The GitHub Pages workflow builds them and publishes them with the static web flasher.
